#!/bin/sh
# UserPromptSubmit branch-warn launcher. Always fail-open: if python3 is
# missing the prompt proceeds with no branch hint.

set -eu

if ! command -v python3 >/dev/null 2>&1; then
    exit 0
fi

dir="$(dirname "$0")"
exec python3 "$dir/../scripts/branch_warn_hook.py" "$@"
